-- Creates the database `hbtn_0c_0` in the MySQL server
-- if it already exists, the script shouldn't fail
-- usage of SELECT and SHOW statements are not allowed
CREATE DATABASE IF NOT EXISTS hbtn_0c_0;
