-- inserts a new row in the table first_table of db hbtn_0c_0 which
-- will be passed as an argument of the mysql command.
-- the new row has id = 89 and name = Best School

INSERT INTO first_table(id, name) VALUES (89, 'Best School');
