-- displays the number of records with id=89 in the table `first_table` in a db in the MySQL server.
-- the db name is passed as an argument of the mysql command

SELECT COUNT(*) FROM first_table WHERE id = 89;
