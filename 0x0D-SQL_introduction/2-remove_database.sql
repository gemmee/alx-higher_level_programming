-- deletes the database `hbtn_0c_0` in the MySQL server
-- if the db doesn't exist, the script shouldn't fail
-- usage of SELECT and SHOW statements are not allowed
DROP DATABASE IF EXISTS hbtn_0c_0;
