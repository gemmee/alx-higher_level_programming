-- prints the full description of the table `first_table` from a db in the MySQL server.
-- the db name is passed as an argument of the mysql command
-- usage of `DESCRIBE` or `EXPLAIN` statements is not allowed
-- DESCRIBE first_table;
-- SHOW COLUMNS FROM first_table;

SHOW CREATE TABLE first_table;
